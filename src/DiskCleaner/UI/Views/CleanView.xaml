<Page
    x:Class="DiskCleaner.UI.Views.CleanView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button x:Name="ScanBtn" Content="开始扫描" Width="120" Margin="0,0,10,0"/>
            <Button x:Name="CancelBtn" Content="取消" Width="80" Margin="0,0,10,0" IsEnabled="False"/>
            <Button x:Name="CleanBtn" Content="清理选中" Width="120" Margin="0,0,10,0"/>
            <Button x:Name="CleanAllBtn" Content="清理全部" Width="120"/>
        </StackPanel>
        
        <DataGrid x:Name="FilesGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="0,0,0,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="文件名" Binding="{Binding FileName}" Width="250"/>
                <DataGridTextColumn Header="大小" Binding="{Binding FormattedSize}" Width="100"/>
                <DataGridTextColumn Header="类型" Binding="{Binding FileType}" Width="100"/>
                <DataGridTextColumn Header="路径" Binding="{Binding Directory}" Width="400"/>
                <DataGridTextColumn Header="最后访问" Binding="{Binding LastAccessTime, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock x:Name="StatusText" Text="总计：0 个文件"/>
        </StackPanel>
    </Grid>
</Page>
