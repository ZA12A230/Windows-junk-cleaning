<Page
    x:Class="DiskCleaner.UI.Views.UninstallView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="clr-namespace:DiskCleaner.UI.ViewModels"
    xmlns:models="clr-namespace:DiskCleaner.Models">
    
    <Page.DataContext>
        <vm:UninstallViewModel/>
    </Page.DataContext>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 工具栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="刷新列表" Command="{Binding LoadSoftwareListCommand}" Width="100" Margin="0,0,10,0"/>
            <Button Content="卸载选中" Command="{Binding UninstallSelectedCommand}" Width="120" Margin="0,0,10,0"/>
            <Button Content="导出 CSV" Command="{Binding ExportToCsvCommand}" Width="100"/>
        </StackPanel>
        
        <!-- 软件列表 -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding SoftwareList}"
                  SelectedItem="{Binding SelectedSoftware}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="0,0,0,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding DisplayName}" Width="250"/>
                <DataGridTextColumn Header="发布者" Binding="{Binding Publisher}" Width="200"/>
                <DataGridTextColumn Header="版本" Binding="{Binding DisplayVersion}" Width="100"/>
                <DataGridTextColumn Header="大小" Binding="{Binding FormattedSize}" Width="100"/>
                <DataGridTextColumn Header="安装日期" Binding="{Binding InstallDate}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- 状态栏 -->
        <TextBlock Grid.Row="2" Text="{Binding StatusText}"/>
    </Grid>
</Page>
