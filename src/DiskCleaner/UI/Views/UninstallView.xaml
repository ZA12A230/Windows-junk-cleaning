<Page
    x:Class="DiskCleaner.UI.Views.UninstallView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button x:Name="RefreshBtn" Content="刷新列表" Width="100" Margin="0,0,10,0"/>
            <Button x:Name="UninstallBtn" Content="卸载选中" Width="120" Margin="0,0,10,0"/>
            <Button x:Name="ExportBtn" Content="导出 CSV" Width="100"/>
        </StackPanel>
        
        <DataGrid x:Name="SoftwareGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="0,0,0,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding DisplayName}" Width="250"/>
                <DataGridTextColumn Header="发布者" Binding="{Binding Publisher}" Width="200"/>
                <DataGridTextColumn Header="版本" Binding="{Binding DisplayVersion}" Width="100"/>
                <DataGridTextColumn Header="大小" Binding="{Binding FormattedSize}" Width="100"/>
                <DataGridTextColumn Header="安装日期" Binding="{Binding InstallDate}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <TextBlock x:Name="StatusText" Grid.Row="2"/>
    </Grid>
</Page>
